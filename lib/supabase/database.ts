/**
 * Placeholder Database type for Supabase.
 *
 * Replace this interface with the generated types from
 * `npx supabase gen types typescript` once migrations are
 * applied in Step 8+.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface Database {
  // Tables will be defined here after migrations are created.
}
